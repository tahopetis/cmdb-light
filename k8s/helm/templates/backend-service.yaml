apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmdb-lite.fullname" . }}-backend
  labels:
    {{- include "cmdb-lite.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cmdb-lite.backend.selectorLabels" . | nindent 4 }}